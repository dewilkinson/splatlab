/bin/
/build/
/out/
.vs/
*.user
*.sdf
*.opensdf
*.suo
*.obj
*.pdb
*.ilk
*.exp
*.lib
# ...except the intentionally-committed prebuilt Cauldron libraries (see CAULDRON_USE_PREBUILT in the
# root CMakeLists.txt) -- these are real, checked-in binaries (via Git LFS), not build byproducts.
!/libs/cauldron-prebuilt/lib/**
*.dxo
*.err
CMakeCache.txt
CMakeFiles/
cmake_install.cmake
models/
# data/ and datasets/ are pre-existing local scratch folders from before the "models/" convention above
# (the app's own search paths no longer reference either name) -- kept ignored rather than merged
# automatically, since their contents haven't been individually reviewed.
data/
datasets/

